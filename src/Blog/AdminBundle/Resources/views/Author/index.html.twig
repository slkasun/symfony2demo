{% extends 'AdminBundle::layout.html.twig' %}

{% block section -%}
    <h1>Author list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Createdat</th>
                <th>Id</th>
                <th>Name</th>
                <th>Slug</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blog_admin_author_show', { 'id': entity.id }) }}">{{ entity.createdAt|date('Y-m-d H:i:s') }}</a></td>
                <td>{{ entity.id }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.slug }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blog_admin_author_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blog_admin_author_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blog_admin_author_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
